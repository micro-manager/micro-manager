install:
	if  [ ! -d $(IJPATH)/oldcfg ]  ; then mkdir $(IJPATH)/oldcfg; fi;
	if [ -e $(IJPATH)/MMConfig.cfg ] ; then mv $(IJPATH)/*.cfg $(IJPATH)/oldcfg; fi;
	cp MMConfig_demo.cfg $(IJPATH)
	cp *.html $(IJPATH)
	cp MMStartup.bsh $(IJPATH)
